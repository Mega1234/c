#include<stdio.h>
#include<conio.h>
void main()
{
int a[20],b,i;
float sum=0,meam=0;
printf("enter the number of terms:");
scanf("%d",&b);
printf("enter the number of values:");
scanf("%d",&a);
for(i=0;i<b;i++);
{
sum=sum+a[i];
mean=sum/b;
}
printf("%d",&a);
getch();
}
